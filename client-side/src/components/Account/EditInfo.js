import React from 'react';
import Aux from '../../hoc/Aux/Aux';
import Input from '../UI/Input/Input';

const editInfo = (props) => {
  let editInfoForm = null;
  
  return (
  <Aux>
    {editInfoForm}
  </Aux>
  )
}

export default editInfo;